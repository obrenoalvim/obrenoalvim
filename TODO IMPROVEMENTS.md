# TODO Improvements

### Orphaned profile-art assets (info-card.svg, contrib-heatmap.svg)
- **Category:** Refactor
- **What:** Both SVGs were deliberately removed from README.md/README.pt.md (commit `46034d0`, "docs: remove contribution heatmap and info card from README") but the files themselves, the scripts that generate them (`scripts/render_heatmap_svg.py`, `scripts/make_info_card.py`), and the daily cron workflow (`.github/workflows/update-profile-art.yml`) were never cleaned up. The workflow still runs every day, regenerates `contrib-heatmap.svg`/`data/contributions.json`, and auto-commits ("chore: refresh contribution graph [skip ci]") — 15+ such commits in history for an artifact no page references. `info-card.svg` is also stale: it hardcodes `Now: Movida`, which predates the README's current "Currently building BloodLink" line, and is never regenerated by CI (`make_info_card.py` isn't called by the workflow, so it's manually run or dead code).
- **Where:** `.github/workflows/update-profile-art.yml`, `scripts/render_heatmap_svg.py`, `scripts/make_info_card.py`, `contrib-heatmap.svg`, `info-card.svg`, `data/contributions.json`
- **Why:** Either the cards are worth keeping (re-add to README, fix "Movida" → current project, wire `make_info_card.py` into the workflow so it doesn't go stale again) or they're not (delete the files, the scripts, and the workflow, saving a daily CI run for nothing). Left as a judgment call since it's a content/design decision about what the profile README should show, not a safe mechanical fix.
- **Risk:** Low — either path is reversible via git history.
- **Effort:** Low
